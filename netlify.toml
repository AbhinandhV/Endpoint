[build]
  base = "endpoint-dashboard"
  command = "npm run build"
  publish = "endpoint-dashboard/build"

# SPA redirect — all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
